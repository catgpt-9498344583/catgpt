services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    volumes:
      - frontend-dist:/app/dist
    command: ["sleep", "infinity"]

  backend:
    depends_on:
      - frontend
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    volumes:
      - ./backend:/app
      - frontend-dist:/app/static
    ports:
      - "127.0.0.1:5000:5000"

volumes:
  frontend-dist:
